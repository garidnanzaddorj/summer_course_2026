// function parameter (x, y, color)
// drawBall функцыг дараах параметр ашиглан
// хийхдээ өнгийг нь дураараа сонгодог болгорой
function drawBall(x, y, color) {
    fill(color);
    circle(x, y, 50);

}
// function parameter (x, y, color)
// drawBall функцыг дараах параметр ашиглан
// хийхдээ өнгийг нь дураараа сонгодог болгорой
function drawBall(x, y, color) {
    fill(color);
    circle(x, y, 50);

}